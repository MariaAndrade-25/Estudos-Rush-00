
<h1> Estudos-Rush-00</h1>

📖 Estudos em grupo para a atividade do Rush 📖

**Então vamos lá**

 Vamos usar a biblioteca então começamos sempre com:  #include <unistd.h>

 Quando escrevemos (*void*) faz com que a função não retorne valor e sim execute uma função

*ft_purchar* = Serve para "colocar caractere"

*char x* = armazena um caractere --> x é a variável e pode ser qq letra

*while* = Uma condição que geralmente acompanha uma variável

*if* (se): Avalia uma condição. Se for verdadeira, executa o bloco de código.

*else* (senão): É o "plano B". Ele só é executado se a condição do if for falsa.

## 🛠️ Compilação e Testes

Como a 42 exige flags específicas de segurança, todos os arquivos foram testados com:

```bash
gcc -Wall -Wextra -Werror seu_arquivo.c -o programa && ./programa
